interface:
  display_name: "Codex Owner Operator"
  short_description: "Owns projects and verifies worker outputs."
  default_prompt: "Use the codex-owner-operator skill. Act as Owner for this project. Run the company integrity gate before project work, verify evidence, supervise workers only after integrity passes, and continue from the current next task."
